# Core data / numerics
pandas>=2.0
numpy>=1.24
scipy>=1.11
pyarrow>=14.0          # parquet read/write

# Machine learning (Research Agent)
scikit-learn>=1.3
xgboost>=2.0
ruptures>=1.1          # PELT change-point detection
hmmlearn>=0.3          # HMM (benchmarked, not primary)

# Validation
pydantic>=2.0

# Data sources
fredapi>=0.5           # FRED macro series + DGS10
wrds>=3.1              # WRDS/CRSP ETF returns, FF factors, market cap
requests>=2.28         # BLS OES zip download

# LLM
anthropic>=0.25        # Claude API (rationale + reasoning trace)

# Testing
pytest>=7.0

# Optional — kept for data/fetch/prices.py (not used by main pipeline)
yfinance>=0.2
